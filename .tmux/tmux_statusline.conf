#
# tmux status line configuration
#

# ステータスライン表示
set-option -g status on
# ステータスライン更新間隔(秒):default 15sec
set -g status-interval 5
# UTF-8
set-option -g status-utf8 on
# ステータスラインの上下位置(top/bottom) - tmux 1.7 or later
set -g status-position bottom
# ステータスラインの左右位置 (left/centre/right):default left
set-option -g status-justify left


## カラー設定
#
# Color名称
#  black, red, green, yellow, blue, magenta, cyan, white
#  brightblack, brightred, ..., brightxxxxx
#
# Reference example to set colors
#  - https://github.com/seebi/tmux-colors-solarized
#

# default statusbar colors
set-option -g status-bg colour235 #base02
set-option -g status-fg colour136 #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg colour244 #base0
set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg colour166 #orange
set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
set-option -g pane-border-fg colour235 #base02
set-option -g pane-active-border-fg colour240 #base01

# active pane border
#set -g pane-active-border-fg brightgreen
#set -g pane-active-border-bg black

# message text
set-option -g message-bg colour235 #base02
set-option -g message-fg colour166 #orange

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# copy mode colors
set-window-option -g mode-fg black
set-window-option -g mode-bg white

# clock
set-window-option -g clock-mode-colour colour64 #green


## ステータスラインの表示情報
#
# Special character sequences
#  #(shell-command) コマンド実行結果(1行目のみ)
#  #[attributes] カラーor属性 変更
#  #H ローカルホスト名
#  #h ローカルホスト名(ドメインなし)
#  #F カレントウィンドウのフラグ
#  #I カレントウィンドウ番号
#  #P カレントペイン番号
#  #S セッション名
#  #T カレントペインタイトル
#  #W カレントウィンドウ名
#  ## #文字
#
# 日付
#  #y 年 (西暦の下二桁)
#  #m 月
#  #d 日
#  #H 時 (24時間表示)
#  #M 分
#  #S 秒


#
# ステータスライン - Left
#
set -g status-left " #[fg=black,bold]#I-#P #20(whoami)#[default] >"

# Leftの表示領域の最大長
set -g status-left-length 20


#
# ステータスライン - Right
#
set -g status-right '< #[fg=black,bold]#(echo $LANG)#[default] \
< #[fg=blue]up #(pc-running-time)#[default] \
< #[fg=magenta](#(loadaverage)) Mem #(used-mem)%%#[default] \
< #[fg=red,bold]%y/%m/%d(%a) %H:%M:%S#[default] '

# Rightの表示領域最大長
set -g status-right-length 90


#
# tmux-powerline
#
# https://github.com/jedipunkz/tmux-powerline
#

# Left status line
if "test -d ~/.tmux/tmux-powerline" "set-option -g status-left-length 80"
if "test -d ~/.tmux/tmux-powerline" "set-option -g status-left '#(~/.tmux/status-left.sh)'"

# Right status line
if "test -d ~/.tmux/tmux-powerline" "set-option -g status-right-length 100"
if "test -d ~/.tmux/tmux-powerline" "set-option -g status-right '#(~/.tmux/status-right.sh)'"

