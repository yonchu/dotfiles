#!/bin/sh

repo_list=~/dotfiles.local/repo_list.txt

if [ ! -f "$repo_list" ]; then
    echo "Not found $repo_list"
    exit 1
fi

git-check-status "$repo_list"

echo

git-check-remote-update "$repo_list"
